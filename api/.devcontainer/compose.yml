version: '3'

services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile.dev
    volumes:
      - ../../:/workspace:cached
      - /mnt/optssd/docker_data/hinav-book-thum:/opt/product_thum:ro
    command: sleep infinity
    environment:
      SQLALCHEMY_DATABASE_URL: "postgresql://postgres:password@db:5432/mydatabase"
      DATA_ROOT: /workspace/api/.data-dev
      APP_ROOT: /workspace/api
      IS_DEV: true
      DEBUG_LOG: true
      SECRET_KEY: 54795a8aeea928c6a134d6075299b46905ec03b1077117e3bfbea8eaee0f38a4

  db:
    image: postgres:latest
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: mydatabase
      POSTGRES_PASSWORD: password

volumes:
  postgres-data:
